== Simple Bootstrap App Demo

This is a demo app to demonstrate long asset precompiling times.

The issue happens on a Windows system. I haven't tested on other systems at this point.

Run "rake assets:precompile -RAILS_ENV=production"